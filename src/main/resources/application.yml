server:
  error:
    path: /error
spring:
  mvc:
    pathmatch:
      matching-strategy: ant_path_matcher
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root
    url: jdbc:mysql://localhost:4306/uopp
    password: root
  mail:
    host: localhost
    username: phello
    password: uhello
    port: '4025'
  jpa:
    properties:
      hibernate:
        format_sql: 'true'
    hibernate:
      ddl-auto: update
jwt:
  secret: 'test-secret'